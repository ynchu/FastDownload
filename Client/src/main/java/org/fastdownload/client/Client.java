package org.fastdownload.client;

import javafx.application.Application;
import org.fastdownload.client.gui.model.ClientStart;

public class Client {
    public static void main(String[] args) {
        Application.launch(ClientStart.class, args);
    }
}
